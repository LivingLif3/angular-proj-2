import { ZoomPhotoDirective } from './zoom-photo.directive';

describe('ZoomPhotoDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomPhotoDirective();
    expect(directive).toBeTruthy();
  });
});
